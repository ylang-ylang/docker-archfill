# softether-vpnserver

Builds a multi-architecture SoftEther VPN Server image from the upstream stable
source release tag.

Supported platforms:

- `linux/amd64`
- `linux/arm64`

Set `image` in `recipe.yaml` to your Docker Hub namespace before publishing.

